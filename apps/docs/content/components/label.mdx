# Label

Accessible form label with required indicator and disabled state.

## Import
```tsx
import { Label } from '@soraui/react';
```

## Usage
```tsx
<Label htmlFor="email" required>Email Address</Label>
```

## Props
- `required`: `boolean` (default: `false`)
- `disabled`: `boolean` (default: `false`)
