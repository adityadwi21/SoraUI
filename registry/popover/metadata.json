{
  "name": "popover",
  "type": "registry:ui",
  "level": 2,
  "description": "Floating overlay panel anchored to a trigger. Accessible with click-outside, Escape key, and optional focus trap.",
  "props": {
    "open": { "type": "boolean" },
    "defaultOpen": { "type": "boolean", "default": false },
    "placement": { "type": "Placement", "default": "bottom" },
    "offset": { "type": "number", "default": 8 },
    "trapFocus": { "type": "boolean", "default": false }
  },
  "accessibility": {
    "keyboard": ["Space/Enter to toggle trigger", "Escape to dismiss", "Tab navigation"],
    "aria": ["role=\"dialog\"", "aria-haspopup=\"dialog\"", "aria-expanded on trigger"],
    "focus": "Optional focus trapping, restores focus to trigger on close"
  },
  "files": [{ "path": "components/ui/popover.tsx", "type": "registry:ui" }],
  "dependencies": ["@soraui/hooks"],
  "installation": { "cli": "npx soraui add popover", "npm": "import { Popover, PopoverTrigger, PopoverContent, PopoverClose } from '@soraui/react'" }
}